<!DOCTYPE html>
<html>
<head>
  <title>Barebonesboard</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'BarebonesBoard', root_url, :class => 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="barebones-collapse">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to 'Home', root_url %></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>Hello, <%= current_user.username %></li>
        <% else %>
          <li><a id="login" href="#">Login</a></li>
          <li><p class="navbar-text">or</p></li>
          <li><a id="create-account" href="#">Create an Account</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
 <p class="notice"><%= notice %></p>
 <p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
